import type { FC } from "react";
import Navbar from "../navbar";
import "../../app.css";

const Landing: FC = ({}) => {
  return (
    <div>
      <Navbar />
      <h1>Landing Page</h1>
    </div>
  );
};

export default Landing;
